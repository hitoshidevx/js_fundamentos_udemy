const [a, b, c , d] = [3, 5, 11, 15]

const soma = a + b + c + d
const multiplicacao = b * d
const subtracao = c - a
const divisao = d / b
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)